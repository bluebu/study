# 构建产物一律不进库（由 GitHub Actions 在部署时生成）
dist/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# 本地素材（作业照片 / 录音，不上传）
inbox/

# 喂数据台（../feeder）产出的核对图：只用来核一眼红线框对不对，
# 看完就没用了，而且能从原图重新生成。数据本身（.read.json / .ref.txt）要进 git ——
# 录音和教材照片不进仓库，那些测量再也算不出来。
*.marked.png
